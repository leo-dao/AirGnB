import React from "react";
import styled from "styled-components";

const RenterInfo = () => {
    return (
        <div>
            <h1>Renter Info</h1>
        </div>
    );
};

export default RenterInfo;