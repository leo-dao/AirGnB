import React from "react";
import styled from "styled-components";

const LeaserInfo = () => {
    return (
        <div>
            <h1>Leaser Info</h1>
        </div>
    );
};

export default LeaserInfo;